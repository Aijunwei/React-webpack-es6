{
  "name": "react-webpack",
  "scripts": {
    "build": "webpack",
    "dev": "webpack-dev-server --devtool eval --progress --colors --hot --content-base build",
    "watch": "webpack-dev-server --hot --inline  --progress --colors --content-base build"
  },
  "devDependencies": {
  "babel-core": "^5.5.8",
    "babel-loader": "^5.1.4",
     "react": "^0.13.3",
    "jsx-loader": "^0.13.2"
  }
}
